#include "main.h"
#include<stdio.h>
/**
 * main - Entry point
 *
 * _puts - writes a line of string to the output
 *
 * Return: Always 0 (Success)
 */
void _puts(char *s);
int main(void)
{
	puts("\"Programming is like building a multilingual puzzle");
	return (0);
}

